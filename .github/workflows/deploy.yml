name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install dependencies for VAK
        run: npm ci
        working-directory: ./src/vak_assessment_nextjs
      
      - name: Build VAK
        run: npm run build
        working-directory: ./src/vak_assessment_nextjs
      
      - name: Install dependencies for Communication
        run: npm ci
        working-directory: ./src/communication_style
      
      - name: Build Communication
        run: npm run build
        working-directory: ./src/communication_style
      
      - name: Create deployment structure
        run: |
          mkdir -p deploy/vak
          mkdir -p deploy/communication
          cp -r src/vak_assessment_nextjs/out/* deploy/vak/
          cp -r src/communication_style/out/* deploy/communication/
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
